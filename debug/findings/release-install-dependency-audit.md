# Release-install dependency audit

- Symptom: the source checkout passed `npm audit --omit=dev`, while a clean `v0.2.1` GitHub Release installation reported moderate advisory `GHSA-w5hq-g745-h8pq` on `uuid@8.3.2` through `exceljs@4.4.0`.
- Root cause: npm applies `overrides` only from the installation root. The advisor package's reviewed `uuid: ^11.1.1` override protected repository builds but was ignored when the Release archive became a dependency of the bootstrap-created consumer root.
- Safety impact: repository-only audit evidence overstated the security of the installed artifact. Functional Release smoke could pass while the consumer graph still contained an advisory.
- Downstream containment: both bootstraps retain the checksummed archive and create a private manifest only inside their dedicated installation root, reject an unrelated pre-existing manifest, record exact reusable dependency entries, apply the reviewed uuid override before resolution, require the installed version to be at least 11.1.1, and fail closed when `npm audit --omit=dev --audit-level=low` finds any runtime advisory.
- Cross-platform findings: public `v0.2.2` passed macOS and Ubuntu but failed on Windows because verification assumed a root-hoisted `node_modules/uuid`. Public `v0.2.3` followed the advisor-to-ExcelJS-to-uuid resolution chain and again passed macOS and Ubuntu, but Windows npm rewrote the archive dependency to a link to the CI checkout when invoked from that checkout with `--prefix`; only eight packages were installed and ExcelJS was missing. `v0.2.4` runs install and audit with the isolated consumer root as npm's actual working directory, rejects any manifest or lock entry that no longer names the retained GitHub Release archive, and retains failure-only CI summaries of the consumer manifest, advisor lock entry, and dependency tree.
- Release evidence: public `v0.2.4` asset `sustech-course-advisor-0.2.4.tgz` has SHA-256 `90e4b22bc2b44cfa3f6fa0c8b0aac045eb7ca1f77e3c7fe7ad011aef01ae06c5`. [Release smoke run 33609474779](https://github.com/Stevvven777/sustech-course-advisor/actions/runs/33609474779) installed it successfully on fresh macOS, Ubuntu, and Windows runners. An independent Darwin arm64 default-URL installation matched the public checksum, retained the archive and manifest at mode `0600`, retained launchers at `0700`, installed 105 packages, resolved `exceljs@4.4.0 -> uuid@11.1.1`, passed all 29 packaged tests (28 passed and the Windows-only test skipped), reported zero runtime audit findings, and reached `installationReady`, `authenticationReady`, and `readyForPersonalizedPlanning` after a read-only live auth check.
- Latest evidence: public `v0.2.5` asset `sustech-course-advisor-0.2.5.tgz` has SHA-256 `3a573a55661db0466f7f092c07bff9f532404d8e6a20afff77e20e641b66c8bc`. [Release smoke run 33617123408](https://github.com/Stevvven777/sustech-course-advisor/actions/runs/33617123408) installed it successfully on fresh macOS, Ubuntu, and Windows runners. A separate Darwin arm64 default-URL install matched that public digest, installed 105 packages, resolved `sustech-course-advisor@0.2.5 -> exceljs@4.4.0 -> uuid@11.1.1` beside `sustech-cli@0.10.0`, and reported zero findings when auditing the actual consumer lockfile. The advisor remains absent from npm; npm is only the upstream CLI and dependency-resolution channel.
- Maintenance rule: audit both the source lockfile and the bootstrap-created consumer lockfile. Never assume that `overrides` declared by a package will affect downstream consumers.
