# Public v0.2.7 Darwin arm64 validation

- Scope: validate the published GitHub Release and Skill workflow from a fresh isolated installation root on a real Darwin arm64 host. The source checkout, candidate tarball, npm publication, and older installed versions were not accepted as substitutes for the public asset.
- Artifact evidence: [GitHub Release v0.2.7](https://github.com/Stevvven777/sustech-course-advisor/releases/tag/v0.2.7) was built from tagged main commit `75e603f` by [release run 33635272304](https://github.com/Stevvven777/sustech-course-advisor/actions/runs/33635272304). The retained archive matched its sibling checksum and GitHub asset digest `sha256:c6911fb74a0d3ac381771ed92e085e16d94261b8cd2ae0b2fc8246161f5a01da`.
- Cross-platform evidence: version-bound [Release smoke run 33635448344](https://github.com/Stevvven777/sustech-course-advisor/actions/runs/33635448344) used a separate version-resolution job to check the workflow revision against the requested version, while each matrix installation job checked out the exact v0.2.7 tag and installed the public asset successfully on macOS, Ubuntu, and Windows. Merged-main [CI run 33635004109](https://github.com/Stevvven777/sustech-course-advisor/actions/runs/33635004109) passed type, test, Skill, privacy, bootstrap, whitespace, and package-content gates on all three platforms.
- Environment evidence: the public bootstrap detected Darwin arm64, reused compatible Node.js v26.8.1, changed neither PATH nor the system runtime, installed 105 packages, selected its installation-root npm cache at mode `0700`, retained its manifest and archive at mode `0600`, and verified advisor v0.2.7 plus `sustech-cli@0.10.0`.
- Functional evidence: the public installation's packaged suite discovered 32 tests on Darwin; 31 executed and passed, while the Windows-only launcher test skipped. The executed coverage included a child that ignored `SIGTERM`; the default deadline returned `COMMAND_TIMEOUT`, escalated cleanup, and verified the child PID disappeared. Separate CLI checks created a private schema v2 Profile from exactly eleven redirected synthetic answers, migrated schema v1, refreshed a synthetic Profile, ran cached planning twice, ran a projected synthetic live workflow, audited, rendered HTML/XLSX and three strategy ICS files twice, and generated an exact selection preview. A colliding output path failed before overwriting Profile input.
- Privacy and mutation boundary: Profile, projected cache, plan, HTML, XLSX, and all ICS artifacts retained mode `0600`. Export inspection found no credential, student identifier, password, token, cookie, raw-payload, or injected sentinel fields. The command log contained `tis degree progress` without `--details`, course and NCES reads, and `tis selection preview`; no enroll, cart, drop, apply, or other campus mutation was invoked.
- Real Keychain and network evidence: status-only access to the real `macos-keychain` returned in 156 milliseconds with backend and credential availability true; no SID or secret was printed. The current read-only live auth check reached the ten-second `COMMAND_TIMEOUT` boundary. A same-condition public v0.2.6 comparison also timed out, while an earlier check through the process-scoped inherited route passed in 1.744 seconds, so the failure is recorded as intermittent TIS/CAS availability rather than credential expiry or a v0.2.7 regression. No login or repeated retry was started.
- Real refresh failure evidence: a v0.2.7 candidate call through the working TIS route reached the default boundary after invoking only `tis degree progress --json`. It emitted no success output and left the private synthetic Profile byte-for-byte unchanged. The successful synthetic path changed only `refreshedAt`, proving both commit and fail-closed behavior without retaining a live academic payload.
- Dependency, license, and publication evidence: the installed consumer tree resolved `sustech-course-advisor@0.2.7 -> exceljs@4.4.0 -> uuid@11.1.1` beside `sustech-cli@0.10.0`; `npm audit --omit=dev --audit-level=low` reported zero vulnerabilities. Source license and third-party notice gates passed. npm does not contain the advisor package, and the repository's documented distribution channel is GitHub Releases.
- Remaining release gate: the local Skill functions, artifact contracts, privacy boundary, timeout cleanup, packaging, and cross-platform installs are ready. A fresh successful public-v0.2.7 live auth check remains pending a responsive TIS/CAS window; do not hide that external gate or treat it as a reason to relogin.
- Maintenance rule: repeat public-asset validation after each Release, use only synthetic academic fixtures in durable evidence, print only projected readiness fields for Keychain/live checks, and separate current campus-route availability from release correctness.
