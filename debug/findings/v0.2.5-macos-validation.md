# Public v0.2.5 Darwin arm64 validation

- Scope: validate the published GitHub Release and Skill workflow from a fresh isolated installation root on a real Darwin arm64 host. The source checkout, npm publication, and an older installed version are not accepted as substitutes for the public asset.
- Artifact evidence: [GitHub Release v0.2.5](https://github.com/Stevvven777/sustech-course-advisor/releases/tag/v0.2.5) was built from tagged main commit `9ee5243`. Its retained archive matched both the sibling checksum and GitHub asset digest `sha256:3a573a55661db0466f7f092c07bff9f532404d8e6a20afff77e20e641b66c8bc`.
- Environment evidence: the bootstrap detected `Darwin arm64`, reused compatible Node.js v26.8.1, changed neither PATH nor the system runtime, installed 105 packages in its isolated consumer root, and verified advisor v0.2.5 plus `sustech-cli@0.10.0`.
- Functional evidence: all 30 packaged tests ran on the installed archive (29 passed and the Windows-only launcher test skipped). Separate CLI checks created a private schema v2 Profile from exactly eleven redirected synthetic answers, migrated a schema v1 fixture, ran cached planning twice, ran a projected synthetic live workflow, audited the plan, rendered HTML/XLSX and three strategy ICS files twice, and generated an exact selection preview through a fake no-write upstream. A path collision failed before overwriting its Profile input.
- Safety impact: a narrow install or doctor check would miss regressions in interview input, migration, cache isolation, export shape, preview boundaries, artifact permissions, or consumer-only dependencies.
- Downstream containment: Profile, cache, plan, HTML, XLSX, and ICS artifacts created by the advisor retained mode `0600`; cached and render-only runs used a deliberately nonexistent campus executable; the synthetic live cache and every exported format were inspected for credential, student identifier, token, cookie, raw-payload, and injected sentinel fields and contained none.
- Live read evidence: a sanitized `doctor --profile default --live` reported installation, authentication, and personalized-planning readiness true, `macos-keychain` available, no missing capability or consequence, and a passed read-only auth check. One subsequent `refresh` invoked `tis degree progress` without `--details`, discarded the upstream response, and changed only the synthetic Profile timestamp; no personal value was printed or persisted.
- Mutation boundary: the observed command log contained `tis selection preview` only. No enroll, cart-update, drop, apply, or other campus-state mutation was invoked.
- Dependency and license evidence: the installed consumer tree resolved `sustech-course-advisor@0.2.5 -> exceljs@4.4.0 -> uuid@11.1.1` beside `sustech-cli@0.10.0`; `npm audit --omit=dev --audit-level=low` reported zero vulnerabilities. The repository runtime-license gate and third-party notices passed before tagging. npm registry returned `E404` for the advisor package, confirming GitHub Releases remains its only publication channel.
- Maintenance rule: repeat this validation from the public asset after every Release, keep all academic fixtures synthetic, print only projected readiness fields for live checks, and stop before any campus mutation. Record platform, digest, exact versions, audit result, output inspection, and workflow run rather than reporting only a successful command exit.
